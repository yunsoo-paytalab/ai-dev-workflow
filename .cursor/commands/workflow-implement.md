# `/workflow-implement [기능명]` 커맨드

**사용 시점**: 기능 구현 작업 시

**참조 파일**: `@feature-implementation.md`

**기능**:

- 테스트 작성 (Test-First)
- 비즈니스 로직 구현
- API 엔드포인트 구현
- 리팩토링

**권장 사항**:

- 설계 문서 검증 완료 (기능 목록 필요)
- API 엔드포인트 설계 완료 (권장)
- 데이터베이스 스키마 설계 완료 (권장)

**사용법**:

```
/workflow-implement 사용자인증
/workflow-implement 상품관리
```

**AI 실행 프롬프트**:

```
기능 구현 작업을 수행해주세요:

1. `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` 파일을 읽어 현재 상태를 확인해주세요
2. `${AGENT_CONFIG_DIR}/rules/workflows/feature-implementation.md` 파일을 읽어 작업 가이드를 확인해주세요
3. 커맨드 파라미터 확인:
   - 기능명: [기능명]

4. 권장 사항 확인:
   - 설계 문서 검증 완료 (기능 목록 필요)
   - API 엔드포인트 설계 완료 (권장)
   - 데이터베이스 스키마 설계 완료 (권장)

   미충족 시 다음을 표시:
```

💡 권장 사항

다음 단계가 완료되지 않았습니다:

- 설계 문서 검증
- API 엔드포인트 설계
- 데이터베이스 스키마 설계

미완료 시 발생 가능한 문제:

- 기능 정보가 불명확할 수 있습니다
- API 스키마를 알 수 없어 구현이 어려울 수 있습니다
- 데이터베이스 엔티티가 정의되지 않았을 수 있습니다

계속 진행하시겠습니까? (y/n)

```

5. 사용자 확인 후 작업 진행
6. `feature-implementation.md`의 프로세스를 따라 작업 수행:
   - Step 1: 기능 정보 확인
   - Step 2: 테스트 작성 (Test-First)
   - Step 3: 비즈니스 로직 구현
   - Step 4: API 엔드포인트 구현
   - Step 5: 리팩토링
   - Step 6: 기능 검수
7. 결과물을 경로에 저장:
   - Spring Boot: `src/main/java/[package]/`, `src/test/java/[package]/`
   - FastAPI: `app/`, `tests/`
   - Flask: `app/`, `tests/`
8. `memory.md` 업데이트:
   - 기능 진행상황 표 업데이트
   - 체크박스 체크: `[x] 기능 구현 - [기능명]`
```
