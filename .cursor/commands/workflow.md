# AI 백엔드 개발 워크플로우 커맨드 가이드

## 개요

**유연한 워크플로우 시스템**:

- 원하는 순서로 작업 가능
- 의존성 미충족 시 경고만 표시
- 사용자 확인 후 계속 진행 가능

**워크플로우 파일**:

- `memory.md` - 진행 상황 추적
- `design-validation.md` - 설계 문서 검증
- `database-schema.md` - 데이터베이스 스키마 설계
- `api-design.md` - API 엔드포인트 설계
- `feature-implementation.md` - 기능 구현
- `system-integration.md` - 통합 및 API 테스트

---

## 메인 커맨드

### `/workflow start`

**사용 시점**: 프로젝트 시작 시

**기능**:

- `memory.md` 읽고 현재 상태 파악
- 설계 문서 확인
- 첫 작업 안내

**사용법**:

```
/workflow start
```

**AI 실행 프롬프트**:

```
다음 단계를 수행해주세요:

1. `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` 파일을 읽어 현재 진행 상황을 파악해주세요
2. 프로젝트 기본 정보가 입력되었는지 확인해주세요
3. 미입력 시: "프로젝트를 시작하기 위해 기본 정보가 필요합니다. `/workflow-design-validation` 커맨드로 시작하시겠습니까?"
4. 입력 완료 시: 현재 상태와 권장 다음 단계를 안내해주세요

권장 작업 순서 (참고용):
1. 설계 문서 검증 (`/workflow-design-validation`)
2. 데이터베이스 스키마 주입 (`/workflow-database-schema`)
3. API 설계 주입 (`/workflow-api-design`)
4. 기능별 구현 (`/workflow-implement [기능명]`)
5. 시스템 통합 (`/workflow-integrate`)
6. API 통합 테스트 (`/workflow-api-test`)
```

---

### `/workflow status`

**사용 시점**: 언제든지

**기능**:

- 전체 진행 상황 표시
- 완료/미완료 작업 목록
- 권장 다음 단계 안내

**사용법**:

```
/workflow status
```

**AI 실행 프롬프트**:

```
다음 정보를 표시해주세요:

1. `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` 파일을 읽어 전체 진행 상황을 파악해주세요
2. 다음 정보를 표시해주세요:

**프로젝트 정보**:
- 프로젝트명: [값]
- 백엔드 프레임워크: [값]
- 데이터베이스: [값]
- 테스트 프레임워크: [값]

**진행 상황**:
- ✅ 완료된 작업: [목록]
- 🔄 진행 중: [목록]
- ⏳ 대기 중: [목록]

**기능 진행상황**:
- [기능별 상태 표시]

**권장 다음 단계**:
- [다음 단계 안내]
```

---

### `/workflow update`

**사용 시점**: Memory 파일 수동 업데이트 필요 시

**기능**:

- `memory.md` 파일 수동 업데이트
- 진행 상황 동기화

**사용법**:

```
/workflow update
```

**AI 실행 프롬프트**:

```
다음 작업을 수행해주세요:

1. `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` 파일을 읽어주세요
2. 현재 프로젝트 상태를 분석해주세요:
   - 완료된 작업 확인
   - 진행 중인 작업 확인
   - 생성된 파일 확인
3. `memory.md` 파일을 업데이트해주세요:
   - 체크리스트 업데이트
   - 기능 진행상황 표 업데이트
4. "Memory 파일이 업데이트되었습니다." 메시지 표시
```

---

### `/workflow help`

**사용 시점**: 커맨드 목록 확인 시

**기능**:

- 사용 가능한 모든 커맨드 표시
- 각 커맨드의 간단한 설명

**사용법**:

```
/workflow help
```

**AI 실행 프롬프트**:

```
다음 커맨드 목록을 표시해주세요:

**메인 커맨드**:
- /workflow start - 프로젝트 시작
- /workflow status - 현재 상태 확인
- /workflow update - Memory 수동 업데이트
- /workflow help - 도움말
- /workflow reset - 프로젝트 초기화

**작업 커맨드**:
- /workflow-design-validation - 설계 문서 검증
- /workflow-database-schema - 데이터베이스 스키마 설계
- /workflow-api-design - API 엔드포인트 설계
- /workflow-implement [기능명] - 기능 구현
- /workflow-integrate - 통합 및 리팩토링
- /workflow-api-test - API 통합 테스트

각 커맨드의 상세 설명과 함께 표시해주세요.
```

---

## 작업 커맨드

### `/workflow-design-validation` - 설계 문서 검증

📄 상세 문서: `@design-validation.md`

### `/workflow-database-schema` - 데이터베이스 스키마 설계

📄 상세 문서: `@database-schema.md`

### `/workflow-api-design` - API 엔드포인트 설계

📄 상세 문서: `@api-design.md`

### `/workflow-implement [기능명]` - 기능 구현

📄 상세 문서: `@feature-implementation.md`

### `/workflow-integrate` - 통합 및 리팩토링

📄 상세 문서: `@system-integration.md`

### `/workflow-api-test` - API 통합 테스트

📄 상세 문서: `@system-integration.md`

---

## 특수 커맨드

### `/workflow reset`

**사용 시점**: 프로젝트 초기화 시

**기능**:

- `memory.md` 초기화
- 모든 진행 상황 리셋
- 처음부터 시작

**사용법**:

```
/workflow reset
```

**AI 실행 프롬프트**:

```
프로젝트를 초기화해주세요:

1. `${AGENT_CONFIG_DIR}/rules/workflows/memory-template.md` 파일을 읽어주세요
2. `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` 파일을 템플릿으로 덮어써주세요
3. "프로젝트가 초기화되었습니다. `/workflow start` 커맨드로 시작하세요." 메시지 표시
```

---

## 사용 예시

### 프로젝트 시작

```
/workflow start
```

### 현재 상태 확인

```
/workflow status
```

### 설계 문서 검증

```
/workflow-design-validation
```

### 데이터베이스 스키마 설계

```
/workflow-database-schema
```

### API 엔드포인트 설계

```
/workflow-api-design
```

### 기능 구현

```
/workflow-implement 사용자인증
/workflow-implement 상품관리
```

### 시스템 통합

```
/workflow-integrate
```

### API 통합 테스트

```
/workflow-api-test
```

### Memory 업데이트

```
/workflow update
```

### 도움말

```
/workflow help
```

---

## 의존성 경고 시스템

### 경고 표시 형식

```
💡 권장 사항

다음 단계가 완료되지 않았습니다:
- [권장 단계명]

미완료 시 발생 가능한 문제:
- [구체적 문제점 1]
- [구체적 문제점 2]

계속 진행하시겠습니까? (y/n)
```

### 의존성 체크 항목

**데이터베이스 스키마 설계**:

- 설계 문서 검증 완료 (도메인 모델 정보)

**API 엔드포인트 설계**:

- 설계 문서 검증 완료
- 데이터베이스 스키마 설계 완료 (엔티티 정보)

**기능 구현**:

- 설계 문서 검증 완료 (기능 목록 필요)
- API 엔드포인트 설계 완료 (권장)

**시스템 통합**:

- 모든 (또는 대부분의 핵심) 기능 구현 완료

**API 통합 테스트**:

- 시스템 통합 완료

---

## 커맨드 실행 시 자동 수행 작업

### 1. Memory.md 읽기

- 현재 진행 상황 파악
- 완료된 작업 확인
- 다음 작업 결정

### 2. 의존성 체크

- 권장 사항 확인
- 미충족 시 경고 표시
- 사용자 확인 후 진행

### 3. 워크플로우 가이드 참조

- 해당 작업의 가이드 파일 읽기
- 단계별 프로세스 따르기

### 4. 작업 수행 및 검수

- 단계별 작업 수행
- 사용자 검수 요청
- 피드백 반영

### 5. 결과물 저장

- 파일 생성/수정
- 적절한 경로에 저장

### 6. Memory.md 업데이트

- 체크리스트 업데이트
- 기능 진행상황 표 업데이트
- 파일 경로 기록

---

## 권장 작업 순서

> 💡 **참고용**: 아래 순서를 따르지 않아도 됩니다. 원하는 순서로 진행하세요.

1. **설계 문서 검증** (`/workflow-design-validation`)
2. **데이터베이스 스키마 주입** (`/workflow-database-schema`)
3. **API 설계 주입** (`/workflow-api-design`)
4. **기능별 반복**:
   - 기능 구현 (`/workflow-implement [기능명]`)
5. **시스템 통합** (`/workflow-integrate`)
6. **API 통합 테스트** (`/workflow-api-test`)

---

## 파일 구조

### 커맨드 파일

**메인 파일**:

- `${AGENT_CONFIG_DIR}/commands/workflow.md` - 메인 커맨드 및 전체 가이드
  - `/workflow start` - 프로젝트 시작
  - `/workflow status` - 현재 상태 확인
  - `/workflow update` - Memory 수동 업데이트
  - `/workflow help` - 도움말
  - `/workflow reset` - 프로젝트 초기화

**작업 커맨드 파일**:

- `${AGENT_CONFIG_DIR}/commands/workflow-design-validation.md` - `/workflow-design-validation`
- `${AGENT_CONFIG_DIR}/commands/workflow-database-schema.md` - `/workflow-database-schema`
- `${AGENT_CONFIG_DIR}/commands/workflow-api-design.md` - `/workflow-api-design`
- `${AGENT_CONFIG_DIR}/commands/workflow-implement.md` - `/workflow-implement [기능명]`
- `${AGENT_CONFIG_DIR}/commands/workflow-integrate.md` - `/workflow-integrate`
- `${AGENT_CONFIG_DIR}/commands/workflow-api-test.md` - `/workflow-api-test`

### 워크플로우 가이드 파일

- `${AGENT_CONFIG_DIR}/rules/workflows/memory.md` - 진행 상황 추적
- `${AGENT_CONFIG_DIR}/rules/workflows/memory-template.md` - Memory 템플릿
- `${AGENT_CONFIG_DIR}/rules/workflows/design-validation.md` - 설계 문서 검증 워크플로우
- `${AGENT_CONFIG_DIR}/rules/workflows/database-schema.md` - 데이터베이스 스키마 설계 워크플로우
- `${AGENT_CONFIG_DIR}/rules/workflows/api-design.md` - API 엔드포인트 설계 워크플로우
- `${AGENT_CONFIG_DIR}/rules/workflows/feature-implementation.md` - 기능 구현 워크플로우
- `${AGENT_CONFIG_DIR}/rules/workflows/system-integration.md` - 통합 및 API 테스트 워크플로우
