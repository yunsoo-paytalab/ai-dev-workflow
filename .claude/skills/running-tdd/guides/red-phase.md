# 🔴 Red Phase - 실패하는 테스트 작성

## 목표

구현 전에 실패하는 테스트를 먼저 작성하여 요구사항을 명확히 정의

## 테스트 작성 원칙

### AAA 패턴

```typescript
it("설명", () => {
  // Arrange (준비) - 테스트 환경 설정
  const input = ...;

  // Act (실행) - 테스트 대상 실행
  const result = targetFunction(input);

  // Assert (검증) - 결과 확인
  expect(result).toBe(expected);
});
```

### 단일 책임

- 하나의 테스트는 하나의 동작만 검증
- 테스트 이름이 실패 원인을 알려줘야 함

### 한글 네이밍

```typescript
// ✅ Good - 한글로 의도 명확히
describe("장바구니", () => {
  it("addItem 호출 시 아이템이 추가되어야 한다", () => {});
  it("동일 상품 추가 시 수량이 증가해야 한다", () => {});
});

// ❌ Bad - 영어로 의도 불명확
describe("ShoppingCart", () => {
  it("should add item", () => {});
});
```

## 테스트 예시

```typescript
describe("장바구니", () => {
  it("addItem 호출 시 아이템이 추가되어야 한다", () => {
    // Arrange
    const cart = new ShoppingCart();
    const item = { id: "1", name: "상품", price: 100 };

    // Act
    cart.addItem(item);

    // Assert
    expect(cart.items).toContainEqual(item);
  });

  it("removeItem 호출 시 해당 아이템이 제거되어야 한다", () => {
    // Arrange
    const cart = new ShoppingCart();
    const item = { id: "1", name: "상품", price: 100 };
    cart.addItem(item);

    // Act
    cart.removeItem("1");

    // Assert
    expect(cart.items).not.toContainEqual(item);
  });
});
```

## 실패 확인

```bash
npm test -- [테스트파일] --watch
```

### ✅ 올바른 실패

```
FAIL  src/__tests__/cart.test.ts
  ● 장바구니 › addItem 호출 시 아이템이 추가되어야 한다
    ReferenceError: ShoppingCart is not defined
```

### ❌ 잘못된 상황

테스트가 바로 통과하면 테스트가 잘못 작성된 것:

- 이미 구현이 존재하거나
- 테스트가 아무것도 검증하지 않음

## Checklist

- [ ] 테스트가 요구사항을 명확히 표현하는가?
- [ ] AAA 패턴을 따르는가?
- [ ] 테스트 이름이 한글로 의도를 설명하는가?
- [ ] 실행 시 예상대로 실패하는가?
