# Technical Debt 문서 작성 가이드

## 핵심 원칙

코드 복잡도, 중복 코드, 테스트 커버리지 등 기술 부채를 식별합니다.

## 분석 항목

1. **복잡도**: 함수별 복잡도 (>10 주의, >20 즉시 개선)
2. **중복 코드**: 10줄 이상 중복 블록
3. **큰 파일**: 함수 >100줄, 클래스 >500줄
4. **깊은 중첩**: 중첩 >4단계
5. **Deprecated API**: var, eval, callback hell
6. **테스트 커버리지**: 파일별 커버리지 (<30% 위험)
7. **문제 주석**: TODO, FIXME, HACK, XXX

## 분석 방법

```bash
# 도구 활용
npm test -- --coverage
npm run lint

# 패턴 검색
grep -rn "TODO\|FIXME\|HACK\|XXX" src/
grep -r "var " src/ --include="*.js"
find . -name "*.js" | xargs wc -l | sort -rn | head -20
```

## 문서 구조

```markdown
# 기술 부채 분석

## 1. 높은 복잡도 함수 (>20)

| 파일                | 함수명         | 복잡도 | 권장 조치 |
| ------------------- | -------------- | ------ | --------- |
| `src/utils/calc.ts` | calculateTotal | 25     | 함수 분리 |

## 2. 중복 코드 블록

| 위치 1      | 위치 2      | 중복 줄 수 | 권장 조치      |
| ----------- | ----------- | ---------- | -------------- |
| file1:10-30 | file2:50-70 | 20줄       | 공통 함수 추출 |

## 3. 큰 파일 (>500 LOC)

| 파일                              | LOC | 권장 조치     |
| --------------------------------- | --- | ------------- |
| `src/components/BigComponent.tsx` | 800 | 컴포넌트 분리 |

## 4. 깊은 중첩 구조

| 파일                  | 위치    | 중첩 깊이 | 권장 조치         |
| --------------------- | ------- | --------- | ----------------- |
| `src/utils/parser.ts` | line 50 | 6단계     | Early return 적용 |

## 5. Deprecated API 사용

| 파일                | API    | 대안       |
| ------------------- | ------ | ---------- |
| `src/legacy/old.js` | var    | const/let  |
| `src/utils/eval.js` | eval() | Function() |

## 6. 낮은 커버리지 파일 (<30%)

| 파일                      | 커버리지 | 우선순위    |
| ------------------------- | -------- | ----------- |
| `src/services/payment.ts` | 15%      | 🔴 Critical |

## 7. 문제 주석 통계

| 유형  | 개수 | 주요 파일         |
| ----- | ---- | ----------------- |
| TODO  | 15   | services/, utils/ |
| FIXME | 8    | components/       |
| HACK  | 3    | legacy/           |

## 8. 우선순위 매트릭스

| 우선순위 | 항목             | 영향도 | 작업량 |
| -------- | ---------------- | ------ | ------ |
| 1        | 결제 로직 테스트 | High   | Medium |
| 2        | 중복 코드 제거   | Medium | Low    |
```
