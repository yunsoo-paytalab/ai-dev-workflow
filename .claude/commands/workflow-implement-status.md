---
name: workflow-implement-status
description: 병렬 구현 중인 Worktree들의 상태를 확인하는 커맨드
version: 1.0.0
---

# /workflow-implement-status $ARGUMENTS

병렬 구현 중인 Feature들의 현재 상태를 확인합니다.

## 인자 처리

> 💡 **Skill**: `@.claude/skills/feature-resolver/SKILL.md` (enableGroupSearch: true)

| 입력 형태  | 예시      | 설명                      |
| ---------- | --------- | ------------------------- |
| Group 이름 | `인증`    | 특정 Group 상태 확인      |
| Group 번호 | `Group 1` | Group 번호로 검색         |
| (없음)     |           | 모든 진행 중인 Group 표시 |

## 실행 프로세스

### Phase 1: 상태 수집

```
1. memory.md에서 병렬 구현 상태 로드
2. 각 worktree의 git 상태 확인
3. 정보 종합
```

### Phase 2: 상태 표시

#### 전체 현황

```
📊 병렬 구현 현황

Group: 인증 (3 Features)
Started: 2024-01-15 10:30
Elapsed: 2h 15m

┌───────────┬─────────────┬─────────┬───────────┬──────────────┐
│ Feature   │ Status      │ Commits │ 변경 파일 │ 마지막 활동  │
├───────────┼─────────────┼─────────┼───────────┼──────────────┤
│ AUTH-001  │ ✅ completed │ 5       │ 12        │ 5분 전       │
│ AUTH-002  │ 🔄 in_progress │ 3    │ 8         │ 2분 전       │
│ AUTH-003  │ 🔄 in_progress │ 1    │ 3         │ 10분 전      │
└───────────┴─────────────┴─────────┴───────────┴──────────────┘

진행률: ████████░░░░░░░░ 33% (1/3 완료)
```

#### Feature 상세 정보 (옵션)

```
📋 AUTH-001 상세

Branch: worktree/AUTH-001
Worktree: .worktrees/AUTH-001
Status: ✅ completed

Commits (5):
  - feat: 로그인 API 연동
  - feat: 로그인 폼 구현
  - test: 로그인 테스트 추가
  - feat: 에러 핸들링
  - refactor: 코드 정리

변경된 파일 (12):
  src/features/auth/login.ts
  src/features/auth/login.test.ts
  src/components/LoginForm.tsx
  ...
```

### Phase 3: 액션 제안

```
다음 액션:

  상태 새로고침:     /workflow-implement-status [Group]
  특정 Feature 상세: /workflow-implement-status [Group] --detail AUTH-001
  병합 시작:         /workflow-implement-merge [Group]
  main 동기화:       /workflow-implement-sync [Group]
```

## 상태 정의

| 상태          | 아이콘 | 설명                  |
| ------------- | ------ | --------------------- |
| `pending`     | ⏳     | 아직 시작 안 함       |
| `in_progress` | 🔄     | 구현 진행 중          |
| `completed`   | ✅     | 구현 완료 (병합 대기) |
| `merged`      | 🎉     | main에 병합 완료      |
| `conflict`    | ⚠️     | 충돌 발생 (해결 필요) |
| `error`       | ❌     | 에러 발생             |

## 옵션

| 옵션                    | 설명                           |
| ----------------------- | ------------------------------ |
| `--detail [Feature ID]` | 특정 Feature의 상세 정보       |
| `--json`                | JSON 형식으로 출력             |
| `--watch`               | 실시간 모니터링 (5초마다 갱신) |

## Git 상태 확인 방법

각 worktree에 대해:

```bash
# 커밋 수 확인
git -C .worktrees/AUTH-001 rev-list --count main..HEAD

# 변경 파일 수 확인
git -C .worktrees/AUTH-001 diff --name-only main..HEAD | wc -l

# 마지막 커밋 시간
git -C .worktrees/AUTH-001 log -1 --format="%ar"

# 현재 작업 상태
git -C .worktrees/AUTH-001 status --short
```

## 에러 처리

### 진행 중인 작업 없음

```
ℹ️ 진행 중인 병렬 구현이 없습니다.

시작하기: /workflow-implement-parallel [Group]
```

### Worktree 누락

```
⚠️ Worktree가 누락되었습니다.

누락된 worktree:
  - .worktrees/AUTH-002 (디렉토리 없음)

복구 옵션:
  1. 해당 Feature 다시 시작
  2. memory.md에서 제거
```

## 결과물

터미널에 상태 테이블 출력 (파일 생성 없음)
